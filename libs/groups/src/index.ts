/**
 * @abroad-matrimony/groups — Future phase stub
 *
 * Service function signatures are declared now so that gateway routes can
 * import them for type safety. Full business logic (regional groups, Intro
 * Drop scheduling, member management) will be implemented in a future phase.
 */

// ── Service stubs ─────────────────────────────────────────────────────────────

/** Future phase: creates a new regional group. */
export async function createGroupService(_data: unknown): Promise<void> {
  throw new Error('createGroupService not yet implemented');
}
