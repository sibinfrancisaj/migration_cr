import { Router } from 'express';

/**
 * Connections router — Phase 5 stub
 *
 * Route handlers (send / accept / decline connection requests) will be wired
 * here in Phase 5. The router is registered now so the gateway starts cleanly
 * and the route namespace `/api/v1/connections` is reserved.
 *
 * @see project-management/epics-stories.md — Phase 5: Connections & Messaging
 */
export const connectionsRouter = Router();
