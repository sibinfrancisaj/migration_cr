export const DISCOVER_ERRORS = {
  INVALID_CURSOR: 'Invalid cursor — must be a value returned from a previous response.',
} as const;

export const DISCOVER_MESSAGES = {
  FEED_FETCHED: 'Discovery feed fetched successfully.',
} as const;
